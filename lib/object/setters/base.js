// Generated by CoffeeScript 1.6.2
(function() {
  var utils;

  utils = require("../../core/utils");

  module.exports = (function() {
    /*
    */
    function _Class(binding) {
      this.binding = binding;
      this._transformer = this.binding._map || this.binding.transform();
    }

    /*
    */


    _Class.prototype.change = function(values) {
      var oldValue, transformedValue;

      transformedValue = this.__transform("to", values);
      if (this._value === transformedValue) {
        return;
      }
      oldValue = this._value;
      this._value = transformedValue;
      return this._change(transformedValue, oldValue);
    };

    /*
    */


    _Class.prototype.bothWays = function() {};

    /*
    */


    _Class.prototype._change = function(value) {};

    /*
    */


    _Class.prototype.__transform = function(method, values) {
      if (!this._transformer) {
        return values[0];
      }
      return utils.tryTransform(this._transformer, method, values);
    };

    return _Class;

  })();

}).call(this);
